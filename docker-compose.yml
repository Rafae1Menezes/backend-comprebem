version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: container_comprebem
    environment:
      MYSQL_ROOT_PASSWORD: passaros
      MYSQL_DATABASE: comprebem_db
      MYSQL_USER: adm
      MYSQL_PASSWORD: 123
    ports:
      - "3306:3306"


